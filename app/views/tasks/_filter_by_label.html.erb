<%= Label.human_attribute_name(:name) %>

<%= form_with url: tasks_path, method: :get, local: true do |f| %>
  <%= f.select("label_id", current_user.labels.pluck(:name, :id), { prompt: @filter_label } ) %>
  <%= f.submit '検索', name: nil %>
<% end %>