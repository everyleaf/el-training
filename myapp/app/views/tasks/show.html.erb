<table class="table">
    <tbody>
        <tr>
            <th>title</th>
            <td><%= @task.title %></td>
        </tr>
        <tr>
            <th>description</th>
            <td><%= @task.description %></td>
        </tr>
        <tr>
            <th>created</th>
            <td><%= @task.created_at %></td>
        </tr>
        <tr>
            <th>updated</th>
            <td><%= @task.updated_at %></td>
        </tr>
    </tbody>
</table>
<%= link_to "Edit", edit_task_path(@task) %>
